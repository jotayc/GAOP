<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imgPEItem"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_margin="10dp"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/casaporras" />

    <TextView
        android:id="@+id/txtPEItem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/imgPEItem"
        android:layout_alignTop="@+id/imgPEItem"
        android:layout_toEndOf="@+id/imgPEItem"
        android:layout_toRightOf="@+id/imgPEItem"
        android:gravity="center"
        android:text="TextView"
        android:textSize="18sp" />
</RelativeLayout>